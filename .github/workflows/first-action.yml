name: CI
on: [workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print greating
        run: echo "Hello, world!"
      - name: Print goodbye
        run: "Done - buy!"
